<h1><%= t('.test_title') %></h1>
<%= render @test %>
<% if @test.questions.present? %>
  <h2><%= t('.test_questions') %></h2>
  <%= render @test.questions %>
<% end %>
<p> <%= link_to t('.add_question'), new_admin_test_question_path(@test) %> 
    <%= link_to t('.back'), admin_tests_path %>
</p>
